# Production API URL for Vite Frontend Deployment
# When hosting on Vercel/Netlify/Render, set this variable to your live Express backend URL.
# Note: Vite environment variables MUST start with the 'VITE_' prefix to be exposed to the client bundle.
VITE_API_URL=https://your-backend.onrender.com
